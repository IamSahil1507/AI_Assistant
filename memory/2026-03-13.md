- Updated VS Code model config to point Awarenet Ollama Proxy at http://127.0.0.1:11435 (avoid IPv6 localhost issues).
- Added VS Code setting github.copilot.chat.ollamaEndpoint=http://127.0.0.1:11435.
- Verified awarenet proxy responds at /api/version (0.6.4) and /api/tags (awarenet:v1).
- Added github.copilot.chat.byok.ollamaEndpoint=http://127.0.0.1:11435 in VS Code settings to satisfy Copilot Chat BYOK Ollama provider.
- Added /api/show endpoint in api/ollama_proxy.py to satisfy Copilot Chat Ollama provider model info requests (requires proxy restart).
- Updated ollama_proxy.py to parse user content arrays (Copilot format) and to return a fallback OpenAI-style response if upstream has no choices.
- Added _openai_fallback_response and now return OpenAI-style responses on upstream errors to avoid Copilot 'no choices' failure.
- Switched upstream calls in ollama_proxy.py to run_in_threadpool with 45s timeout to avoid blocking the event loop (Copilot requests).
- openai_chat now logs requests/responses and returns OpenAI fallback responses on upstream errors to avoid 'no choices'.
- Added SSE streaming support for /v1/chat/completions when stream=true, via _maybe_stream and _response_to_sse.
- For stream=true (Copilot), upstream payload now only includes last user message to reduce prompt size and avoid timeouts.
- Added debug logging for clean_user and requires_confirmation in openai_chat to diagnose confirm loop.
