Rescue pass on backend/operator reliability and verification.

- Fixed broken API wiring in `api/server.py` for operator step construction, desktop artifact routing, voice module import/use, and Awarenet SPA deep links.
- Improved model fallback behavior in `tools/openclaw_bridge.py` so the `assistant` alias prefers a usable smaller discovered model and supports provider-style config entries with `baseUrl` / `api` / `apiKey`.
- Relaxed trivial-request clarification in `tools/task_detector.py` so simple generation prompts like "Say hello in five words" do not bounce unnecessarily.
- Added missing runtime dependencies to `requirements.txt`: `httpx`, `python-multipart`, `pyautogui`, and `pillow`.
- Added regression coverage in `tests/test_api_regressions.py` and `tests/test_bridge_behavior.py` for operator routes, voice/desktop wiring, Awarenet deep links, safe config redaction, provider model flattening, fallback model selection, and task classification.
- Hardened `bridge.get_safe_config()` to redact nested secret values such as `apiKey` before returning config to the UI/API.
- Verified with `.verify-venv`: `unittest` suite green (11 tests) and `compileall` green for `api`, `tools`, and `nexus`.
- Remaining follow-up: editor bridge is still unavailable unless the VS Code/Cursor bridge process is running on `127.0.0.1:18999`; voice listen still needs a Vosk model path configured or auto-discovered.
- Wrote and committed the NEXUS desktop replacement spec at `docs/superpowers/specs/2026-04-03-nexus-native-shell-design.md` with commit `2db7c36` (`docs: add NEXUS native shell design spec`).
- Spec direction: new `NEXUS` native Windows shell from scratch, tray-first with configurable tray/orb/full-screen mission control surfaces, sci-fi visual language, and backend reuse.
- Audited both legacy UIs (`awarenet-ui` React shell and `src/Awarenet.ControlCenter` WinUI shell) and revised the NEXUS spec to require feature parity for models, tasks, memory, policy, approvals, voice, logs, config snapshots, gateway explorer, skills, browser sessions, editor bridge, desktop automation, and native chat workflows.
- Committed the parity revision as `0408a23` (`docs: add Awarenet feature parity to NEXUS spec`).
- Reviewed the user's later spec edits without overwriting them; key planning interpretation is parity-first and backlog-second, because several new bullets are broader than the current Awarenet codebase and are not good cutover blockers.
- Wrote and committed the implementation plan at `docs/superpowers/specs/2026-04-03-nexus-native-shell-implementation-plan.md` as `a71b9c5` (`docs: add NEXUS implementation plan`).
- Started implementing `src/NEXUS.ControlCenter` as a new unpackaged WinUI 3 shell and added it to `Awarenet.ControlCenter.sln`.
- Bootstrapped the NEXUS shell foundation: custom app/theme resources, backend auto-discovery, shell coordinator, settings persistence, and a Mission Control window with sci-fi styling.
- Migrated live NEXUS screens for `Overview`, `Chats`, `Operator`, `Approvals`, `Browser`, `Desktop`, `Voice`, and `Modules` against the existing assistant backend instead of leaving them as static placeholders.
- Found and fixed a hidden WinUI project issue while debugging blank `XamlCompiler.exe` failures: `App.xaml` was being sent to the markup compiler twice after switching to explicit page includes; setting `EnableDefaultApplicationDefinition=false` resolved the base compiler break.
- Verified the new shell by building `src/NEXUS.ControlCenter/NEXUS.ControlCenter.csproj`, building `Awarenet.ControlCenter.sln`, and launching the native app successfully with window title `NEXUS Mission Control`.
- One remaining blocker for this slice: the dedicated shell configuration page still trips WinUI markup compilation, so `Settings` is temporarily left as a placeholder while the rest of the new shell stays shippable.
