- Implemented Awarenet UI clarity pass: summary cards with raw JSON toggles, error banners, gateway diagnostics, and sidebar compact modes config.
- Fixed config persistence by writing updates to user config (~/.openclaw/openclaw.json) and aligned Vite version in awarenet-ui/package.json.
